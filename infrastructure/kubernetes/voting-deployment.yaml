# © 2024 DecVCPlat. All rights reserved.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: decvcplat-voting
  namespace: decvcplat
  labels:
    app: decvcplat-voting
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: decvcplat-voting
  template:
    metadata:
      labels:
        app: decvcplat-voting
        version: v1
    spec:
      containers:
      - name: decvcplat-voting
        image: decvcplat/voting:latest
        ports:
        - containerPort: 8080
        - containerPort: 8081
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: DecVCPlat_ConnectionString
          valueFrom:
            secretKeyRef:
              name: decvcplat-db-secret
              key: connection-string
        - name: DecVCPlat_JwtSecret
          valueFrom:
            secretKeyRef:
              name: decvcplat-jwt-secret
              key: jwt-key
        - name: DecVCPlat_Ethereum_RpcUrl
          valueFrom:
            secretKeyRef:
              name: decvcplat-blockchain-secret
              key: ethereum-rpc
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: decvcplat-voting-service
  namespace: decvcplat
spec:
  selector:
    app: decvcplat-voting
  ports:
  - name: http
    port: 80
    targetPort: 8080
  - name: https
    port: 443
    targetPort: 8081
  type: ClusterIP
