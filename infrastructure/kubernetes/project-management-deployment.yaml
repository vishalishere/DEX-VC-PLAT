# © 2024 DecVCPlat. All rights reserved.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: decvcplat-project-management
  namespace: decvcplat
  labels:
    app: decvcplat-project-management
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: decvcplat-project-management
  template:
    metadata:
      labels:
        app: decvcplat-project-management
        version: v1
    spec:
      containers:
      - name: decvcplat-project-management
        image: decvcplat/project-management:latest
        ports:
        - containerPort: 8080
        - containerPort: 8081
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: DecVCPlat_ConnectionString
          valueFrom:
            secretKeyRef:
              name: decvcplat-db-secret
              key: connection-string
        - name: DecVCPlat_JwtSecret
          valueFrom:
            secretKeyRef:
              name: decvcplat-jwt-secret
              key: jwt-key
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: decvcplat-project-management-service
  namespace: decvcplat
spec:
  selector:
    app: decvcplat-project-management
  ports:
  - name: http
    port: 80
    targetPort: 8080
  - name: https
    port: 443
    targetPort: 8081
  type: ClusterIP
